// Declare a variable
int number = 0;

// Keyword Condition
while (number <= 100) // If condition is true; loop!
{
    //Loop
    // Print number and add 5
    Console.WriteLine(number);
    number = number + 5;
}